class Snake {
  constructor(startPosition, direction, speed) {
    this.position = startPosition;
    this.direction = direction;
    this.speed = speed;
    this.body = [];
    body.push(position);
  }

  update(direction) {}

  getBody() {
    return this.body;
  }
}

export { Snake };
